#include <bits/stdc++.h>
using namespace std;

int main()
{
    // visit command
    while (ss >> word)
    {
        if (word != "visit")
        {
            target = word;
        }
    }

    // __________
    return 0;
}